# yaml-language-server: $schema=https://goreleaser.com/static/schema.json
# vim: set ts=2 sw=2 tw=0 fo=cnqoj

version: 2

before:
  hooks:
    - go mod tidy
    - go generate ./...

builds:
  - id: beans
    main: ./cmd/beans
    binary: beans
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - windows
      - darwin
    ldflags:
      - -s -w
      - -X github.com/hmans/beans/internal/version.Version={{.Version}}
      - -X github.com/hmans/beans/internal/version.Commit={{.ShortCommit}}
      - -X github.com/hmans/beans/internal/version.Date={{.Date}}

  - id: beans-serve
    main: ./cmd/beans-serve
    binary: beans-serve
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - windows
      - darwin
    ldflags:
      - -s -w
      - -X github.com/hmans/beans/internal/version.Version={{.Version}}
      - -X github.com/hmans/beans/internal/version.Commit={{.ShortCommit}}
      - -X github.com/hmans/beans/internal/version.Date={{.Date}}

  - id: beans-tui
    main: ./cmd/beans-tui
    binary: beans-tui
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - windows
      - darwin
    ldflags:
      - -s -w
      - -X github.com/hmans/beans/internal/version.Version={{.Version}}
      - -X github.com/hmans/beans/internal/version.Commit={{.ShortCommit}}
      - -X github.com/hmans/beans/internal/version.Date={{.Date}}

archives:
  - formats: [tar.gz]
    name_template: >-
      {{ .ProjectName }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}
    format_overrides:
      - goos: windows
        formats: [zip]

changelog:
  sort: asc
  use: github
  filters:
    include:
      - "^feat"
      - "^fix"
      - "^perf"
      - "^refactor"
  groups:
    - title: "⚠️ Breaking Changes"
      regexp: "!:"
      order: 0
    - title: Features
      regexp: "^feat"
      order: 1
    - title: Bug Fixes
      regexp: "^fix"
      order: 2
    - title: Performance
      regexp: "^perf"
      order: 3
    - title: Other
      order: 999

release:
  footer: >-

    ---

    Released by [GoReleaser](https://github.com/goreleaser/goreleaser).

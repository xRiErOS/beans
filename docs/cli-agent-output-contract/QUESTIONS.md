# Questions — cli-agent-output-contract

Loose ends. A question here is not yet a decision — it either becomes one (moves to
DECISIONS.md) or gets deliberately deferred with a reason.

| ID | Background | Relevance | Question | Status |
|---|---|---|---|---|
| ~~Q01~~ | Cobra's error-handling order was asserted from documentation, not source. | — | **Resolved** at `cobra@v1.10.2/command.go:1147-1168`. Usage is evaluated after `cmd.execute()` returns (so per-call mutation works), but the check is a **conjunction** over command *and* root — so the fix `beans-ra75` proposes in its implementation note (`cmd.SilenceUsage = false`) is insufficient and would silently break AC-2. See R16. | 🟢 |
| ~~Q02~~ | Who parses `beans update --json`. | — | **Resolved** (T01). Two consumers total; **neither breaks** on the bare form. `fixtures.ts:111` reads `(json.bean?.id ?? json.id)` — already shape-tolerant. The breaking-change premise behind D05 is unsupported. | 🟢 |
| ~~Q03~~ | Scope of the envelope decision. | — | **Resolved** (T01). Nine commands emit JSON in five different shapes; `rename` bypasses `internal/output` entirely and emits two top-level documents. D05 must cover all of them or explicitly exempt them — deciding `update` alone would leave four other shapes standing. | 🟢 |
| Q08 | `beans-src/CLAUDE.md` documents the `rename --json` two-document behaviour as a caveat and states it is "not yet tracked as a bean". | The instruction file is currently teaching agents to work around a defect. If D05 fixes it, the caveat must come out; if D05 exempts it, the caveat is now a deliberate contract and should say so. | Does the `rename --json` two-document emission get fixed under this contract, or ratified as intentional? Either way, who updates CLAUDE.md? | 🟣 |
| Q09 | `delete.go` builds an `output.Response{…}` literal by hand for the multi-bean case rather than using a helper. | Small, but it means the envelope has no single construction point — a contract change would have to find hand-rolled instances too. | Should `internal/output` be the only place that constructs response shapes, enforced by a test? | 🟣 |
| Q04 | M03 shows the JSON error doc goes to **stdout** while the human error goes to stderr. ra75 AC-4 wants errors on stderr "so that --json consumers reading stdout are unaffected". | The two are in tension: today a `--json` consumer reading stdout gets the error *on stdout*, which is arguably right for a JSON protocol and arguably wrong per AC-4. | Should the JSON error document go to stdout (protocol-consistent, current behaviour) or stderr (stream-consistent with AC-4)? Are those two claims reconcilable at all? | 🟣 |
| ~~Q05~~ | `beans query` shape. | — | **Resolved by inspection.** `graphql.go:105` — `--json` on `query`/`graphql` means "no colours", and the payload is the raw GraphQL response. It is a passthrough, not a beans-shaped result, and is **out of scope** for D05. `check` (`check.go:20-23`) is *in* scope and was missed in the first inventory: it declares its own result struct. | 🟢 |
| ~~Q06~~ | Whether the failing `mise build` gates this work. | — | **Resolved.** `go build ./cmd/beans` succeeds, rc=0, ~2s. The frontend embed sits in `internal/web/embed.go` and is only reached by `beans-serve`. CLI work is unblocked; the frontend failure is a separate concern. | 🟢 |
| Q10 | D05 unifies the *success* shape to a bare object. Errors cannot be bare — an error has no bean to be. | The contract is only complete if it says what failure looks like, not just success. Today: `{success:false, error, code}` on stdout (`output.Error`). | Does the error document keep the `{success:false,…}` envelope as the deliberate counterpart to a bare success, and is `success:false` still meaningful once success is signalled by shape rather than by a flag? | 🟣 |
| Q11 | With D07 decided, the stderr side of an error shrinks from 33 lines to 2, which largely defuses D06's double-emission. | Changes the cost/benefit of D06 — a second bean may no longer be warranted. | Is 2 lines of stderr alongside a JSON error document acceptable, or should cobra's error print be silenced entirely (`SilenceErrors`) whenever `--json` is active, leaving stdout as the only channel? | 🟣 |
| ~~Q07~~ | The incident that justifies both beans. | — | **Resolved, two causes, see REFERENCES → okf-tools incident.** (1) The two lost updates **never ran**: a `PreToolUse(Bash)` deny from `git-enforce.py` rejected a compound call over a 52-char commit title, and the reason named only the git rule, so the agent re-ran the git half and dropped four beans mutations. Not a beans defect. (2) The duplicate orphans **were** the envelope: `beans create --json \| … .get('id','')` read `id` at top level where it sits under `bean`, so successful creates read as failures and got re-created. | 🟢 |
| Q12 | Cause 1 is a defect in `~/.claude/hooks/git-enforce.py` / hook ergonomics generally: a `PreToolUse` deny is all-or-nothing over a compound command while its reason string describes one statement. It cost four dropped mutations and two verification rounds. | Higher severity than either bean in this collection, and entirely outside this repo. D03 set the precedent of filing foreign defects here rather than leaving them unfiled. | File it as a bean here per the D03 precedent, or does a defect in the enforcement layer belong somewhere with more authority? And is the fix in the hook (enumerate what was blocked) or in the calling convention (never mix mutations and `git commit` in one Bash call)? | 🟣 |
